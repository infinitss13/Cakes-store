version: '3'


networks:
  internal:

services:
  mongo:
    networks:
      - internal
    image: 'mongo:4.4.6'
    container_name: 'mongo-card-service'
    ports:
      - '27017:27017'
  cakes-store-cart-services:
    ports:
      - '8001:8001'
    build: .
    volumes:
      - ./:/app
    networks:
      - internal

